# 공개 배포 전 TF 재감리 — 2026-09-19

## 검토 범위

현재 공개 저장소와 GitHub Pages, 공개 콘텐츠 JSON, 광고·근거 문구, 제품·후기 경로, 게임 시작과 결과, 정적 배포 및 Worker 배포 조건을 점검했다. 검토자는 소비자 마케팅·여정, 연구 문구·근거, 출시·기술 감리 역할로 나눴다. 이는 AI 역할 기반 교차 검토이며 외부 자격 전문가의 회의나 소비자 조사·법률 승인으로 간주하지 않는다.

기준 소스는 시작 시점 `3dd3aaa`였고, 수정 후 전체 검증을 다시 수행했다. 최신 공개 Pages는 정적 사이트로 작동하지만, 이 사이트에 API·데이터베이스까지 연결된 운영 플랫폼은 아니다.

## 토론 결과와 수정 결정

| 항목 | 확인된 문제 | 적용 |
|---|---|---|
| 게임 시작 전 설명 | 제목의 “뇌 컨디션 확인”이 건강 검사처럼 받아들여질 수 있고, 설명이 게임 규칙을 정확히 말하지 않았다. | 제목 바로 아래에 “색 신호에 맞춰 누르거나 멈추는 1분 게임”이라고 설명하고, 점수가 뇌 피로나 건강 상태를 판단하지 않는다고 계속 표시한다. 결과에는 실제 정답 비율만 보여 준다. |
| 후기 기대 | 사이트가 실제 후기 내용을 보여 주는 것처럼 오해할 수 있었다. 현재 공개 데이터는 인용문이 아니라 후기 링크뿐이다. | 스마트스토어에서 실제 후기 원문을 읽을 수 있다고 제목과 설명에 알린다. 사용자가 지정한 `/products/4701017202#REVIEW_DIALOG` 링크를 유지하고, 허가 없는 후기는 만들지 않는다. |
| 연구 참여자 | Byun 연구 설명이 입면곤란만 겪은 사람을 선별한 것처럼 좁게 읽혔다. Yamatsu 요약에는 잠의 질이 낮은 표본이라는 정보가 충분히 보이지 않았다. | Byun은 “수면 불편을 호소한 성인”으로 고치고, 상세에 PSQI·ISI 기준을 추가한다. Yamatsu는 “수면 설문에서 잠의 질이 낮게 나온 일본 성인”을 요약에 밝힌다. |
| 연구 정보 반복 | Byun·Yoto·Yamatsu 카드에서 대상·용량·기간이 본문 문장과 그림에 되풀이됐다. | 그림이 있는 카드의 첫 문장은 대상과 비교만 안내하고, 용량·기간·절차는 그림에서 먼저 확인하도록 줄였다. |
| 성장호르몬 연구 용어 | 시간 변화 지표를 쉬운 말로 옮기면서 계산 방식과 검사별 통계 결과를 뭉뚱그릴 수 있었다. | 소비자 카드에는 휴식 조건에서 관찰된 두 최고 수치와 운동 후 30분의 두 수치만 쉬운 말로 먼저 보여 준다. 통계 지표 용어는 덜고, 실제 근육 크기·근력은 측정하지 않았다는 경계는 상세 정보에 유지한다. 셀핀다 제품의 효과를 뜻한다고 연결하지 않는다. |
| 제품 접근성 | 연구 목록을 본 뒤 제품 구성으로 이동하려면 홈으로 돌아가야 했다. | 연구 카드와 분리된 페이지 하단에 “연구 내용과 제품 구성은 따로 확인해 보세요” 안내와 가바 1500 구성 링크를 추가했다. 카드 안에 구매 CTA는 두지 않는다. |
| 과거 이력에 남은 경로 | 기존 스캔 정규식이 JSON에 이스케이프된 Windows 경로를 놓쳤다. | 검사기를 보강해 일반·JSON 이스케이프 경로와 일반적인 Unix 사용자·임시 경로를 감지하고, 3개 회귀 검사를 추가했다. 실제 일치 값은 로그에 쓰지 않는다. 현재 스캔은 저장소 내 11개 파일 경로를 경고하며, 그중 이전에 놓친 로컬 매니페스트 기록도 감지한다. 경고는 비차단이고 현재 파일 검사는 차단형이다. Git 이력 재작성이나 force push는 수행하지 않았다. |

## 검증 결과

- `pnpm test`: 101개 통과
- `pnpm run build`: 연구 문구·UI 계약·TF·공개 export 검증 및 production build 통과
- `pnpm run typecheck`: 프런트엔드와 Worker 타입검사 통과
- `node scripts/validate-research-copy.mjs`: 통과
- `node scripts/validate-ui-contract.mjs`: 통과
- `node --test scripts/audit-history-privacy.test.mjs`: 3개 회귀 검사 통과
- `node scripts/audit-history-privacy.mjs`: 비차단 경고 확인. 과거 버전의 경로 형식만 세고 실제 값은 로그로 내보내지 않는다.

정적 Pages 경로(`/cellpinda_GABA/research/`)로 빌드한 화면도 Chromium 152 headless CDP로 확인했다. Browser 플러그인과 프로젝트 Playwright가 없어 CDP를 임시 대체 경로로 썼다. 데스크톱 1440×1400과 모바일 390×844에서 연구 카드 7개와 Powers 결과 우선 표시가 렌더링됐고, 문서 폭은 모바일 화면 폭을 넘지 않았다. 첫 연구의 상세를 열어 참여자·PSQI·ISI 안내를 확인했고 콘솔 오류·경고는 없었다. 화면 캡처는 저장소 바깥 임시 폴더에 두었다. 이는 브라우저 렌더링 검증이지 실제 기기나 소비자 사용성 조사는 아니다.

## 배포 판단과 남은 게이트

GitHub Pages 정적 사이트는 공개 가능하지만 전체 제품 운영 플랫폼은 미완료다. 현재 공개 `/api/health`와 `/api/content`는 404이며 Worker/D1이 연결되지 않았다. 응답 헤더도 HSTS 외 CSP, X-Content-Type-Options, Referrer-Policy, Permissions-Policy를 라이브에서 확인하지 못했다.

티저는 `PREVIEW/HOLD`이며 공개 미디어 파일·자막·권리·표시 승인 증거가 필요하다. 사이트 안에 공개 동의를 받은 실제 후기 인용은 없다. 현재 가격·재고는 스마트스토어에서 확인하도록 남겼다. 실제 주문·취소·환불 대사, 최신 포장 표시사항, 보안 헤더, 외부 전문가·소비자 사용성 테스트도 운영 출시 게이트다.

과거 공개 Git 이력에 남은 경로를 완전히 제거하려면 이력 재작성과 원격 force push가 필요하다. 이 작업은 다른 협업자의 clone·태그·링크에 영향을 줄 수 있으므로 별도 영향 분석과 명시적 승인을 받기 전 수행하지 않는다.

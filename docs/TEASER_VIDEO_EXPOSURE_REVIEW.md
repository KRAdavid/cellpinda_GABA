# 티저 영상 노출 검토

검토일: 2026-09-10  
검토 URL: https://fermented-gaba-documentary-20260903.dubaissday.chatgpt.site/

## 현재 접근 결과

비로그인 상태에서 페이지를 직접 요청한 결과는 다음과 같다.

| 확인 항목 | 결과 | 판단 |
|---|---|---|
| HTTP 응답 | `401 Unauthorized` | 공개 캠페인 링크로 사용할 수 없음 |
| 문서 제목 | `Sign in required` | 첫 접점에서 로그인 장벽 발생 |
| 안내 문구 | `This site uses ChatGPT to securely sign you in` | ChatGPT 계정이 없는 소비자는 영상에 도달하지 못함 |
| 영상 요소 | 비로그인 응답 HTML에서 확인되지 않음 | 실제 영상 길이·자막·메시지 검토 불가 |
| OG 제목/이미지 | ChatGPT 공통 미리보기 | SNS 공유 시 셀핀다 티저로 인식되기 어려움 |
| 캐시 정책 | `no-store` | 공유 미리보기·검색 노출 안정성 확인 불가 |

따라서 이 URL은 현재 소비자 유입용 티저 링크가 아니라 인증된 내부 검토용 링크로 분류한다. 공개 배포 전까지는 홈의 주 CTA, 광고, 카카오톡·인스타그램 공유 링크에 연결하지 않는다.

## 권장 노출 위치

티저는 현재 홈의 공감형 히어로와 1분 리듬 체크를 대체하지 않고, 선택형 이야기 입구로 배치한다.

1. **히어로 보조 CTA** — `40초 티저 보기`를 두 번째 버튼으로 둔다. 첫 CTA는 `1분 리듬 체크`를 유지한다.
2. **인트로 스트립 다음** — `발효가바를 만드는 과정을 40초로 보기` 카드로 영상과 포스터를 노출한다. 이 위치는 ‘나의 상태 발견’ 다음, GABA 상세 설명 전에 해당한다.
3. **영상 종료 화면** — `GABA를 한 문장으로 이해하기`와 `내 하루 리듬 체크` 두 경로를 제공한다. 영상 재생만으로 제품 효능을 암시하지 않는다.

공개 URL은 별도 영상 페이지 또는 모달로 운영하되, 원래 페이지로 돌아오는 링크와 포스터·자막·텍스트 대체 설명을 함께 제공한다.

## 공개 전 영상 승인 기준

- 시작 3초 안에 `오늘, 내 뇌는 쉴 틈이 있었을까?` 또는 이에 준하는 공감 문장이 나와야 한다.
- 제품이 **셀핀다가바 완제품**임을 보여 주고, 원료·발효 공정·연구 자료를 완제품 효능으로 이어 붙이지 않는다.
- `잠이 좋아진다`, `불안이 줄어든다`, `뇌를 진정시킨다`, `치료`처럼 일반식품의 효능을 단정하는 표현은 사용하지 않는다.
- 연구 장면이 들어가면 연구 대상·조건·한계와 `셀핀다가바 완제품의 효과를 보장하지 않음`을 영상 또는 바로 이어지는 설명에서 확인할 수 있어야 한다.
- 마지막 3~5초에는 브랜드, 제품 확인 경로, `근거와 표시사항 보기` 또는 `1분 리듬 체크` CTA를 함께 둔다.
- 음소거 자동재생은 허용하되 소리 자동재생은 하지 않는다. `playsinline`, 재생/일시정지/음량/자막 제어, 키보드 접근, `prefers-reduced-motion` 대체를 제공한다.
- 포스터 이미지와 전체 대본/자막을 별도 제공하고, 제품 사진·연구 이미지·음악·인터뷰의 사용권을 기록한다.

## 노출 측정

영상 노출과 실제 전달을 구분해 다음 이벤트를 익명 집계한다.

| 이벤트 | 의미 |
|---|---|
| `teaser_impression` | 영상 카드가 실제 화면에 들어온 횟수 |
| `teaser_play` | 사용자가 재생을 요청한 횟수 |
| `teaser_progress_25/50/75` | 재생 위치 도달 신호 |
| `teaser_complete` | 끝까지 재생된 신호 |
| `teaser_caption_opened` | 자막을 켠 행동 |
| `teaser_cta_clicked` | 영상 뒤 리듬 체크·GABA 설명으로 이동한 행동 |
| `teaser_exit` | 중도 이탈 신호 |

외부 SNS에 실제 게시·전달됐다고 해석하지 않으며, 개인 답변·이름·계정·영상 URL의 임의 쿼리를 저장하지 않는다.

## 공개 전 필요한 입력

1. 로그인 없이 접근 가능한 최종 URL 또는 공개 MP4/HLS 주소
2. 영상 파일·포스터·자막/대본·총 길이·음원과 이미지의 사용권
3. 영상에 표시한 제품명·내용량·표시사항의 현재 확인본
4. 연구·특허·발효 공정 장면별 출처와 완제품 적용 여부
5. 영상 마지막 CTA의 목적지와 캠페인 측정 기간

현재 URL이 공개 상태로 바뀐 뒤 비로그인 브라우저, 360px 모바일, 소셜 미리보기에서 다시 검토해야 영상의 실제 편집·자막·표현까지 승인할 수 있다.

## 운영 게이트

`data/teaser-manifest.json`이 티저의 현재 상태를 단일하게 기록한다. 현재 상태는 `HOLD`이며, 인증이 필요한 내부 검토 URL만 기록하고 공개 미디어 주소는 비워 둔다. `pnpm run validate:teaser`는 빌드 때 이 URL이 `src/`, `public/`, `index.html`에 새어 나오지 않는지 확인한다. 공개 승인으로 전환하려면 매니페스트에 로그인 없는 HTTPS 미디어 주소와 승인자·승인일을 추가하고, 위 승인 기준을 다시 통과해야 한다. 따라서 현재 공개 배포에는 인증 링크나 검증되지 않은 영상을 자동으로 노출할 수 없다.
